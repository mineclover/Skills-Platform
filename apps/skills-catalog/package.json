{
  "name": "@skills-platform/catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Skills registry, activation catalog, and policy control plane.",
  "bin": {
    "skills-catalog": "src/cli.js"
  },
  "scripts": {
    "check": "tsc --noEmit && node --check src/index.js",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "@skills-platform/contracts": "*",
    "@skills-platform/ledger-store": "*",
    "yaml": "^2.9.0"
  }
}
