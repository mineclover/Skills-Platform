interface:
  display_name: "OpenWiki"
  short_description: "Initialize and update repository OpenWiki docs"
  default_prompt: "Use $openwiki to update this repository's OpenWiki from current source and tests."
policy:
  allow_implicit_invocation: true
dependencies:
  tools: []
