{
  "name": "skills-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the Skills Catalog control plane and the Skills Manager delivery adapter.",
  "workspaces": [
    "apps/skills-catalog",
    "apps/catalog-ui",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build:packages": "npm run build --workspace @skills-platform/contracts && npm run build --workspace @skills-platform/ledger-store && npm run build --workspace @skills-platform/skills-manager-adapter",
    "build": "npm run build:packages && npm run build --workspace @skills-platform/catalog-ui",
    "precheck": "npm run build --workspace @skills-platform/contracts",
    "check": "npm run --workspaces --if-present check",
    "pretest": "npm run build:packages",
    "test": "npm run --workspaces --if-present test"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
